# 使用指南

简体中文 · [English](HELP.en.md)

Rynoa 常驻 Mac 屏幕顶部菜单栏。找不到窗口时，点击菜单栏的 Rynoa 图标；也可以从“应用程序”重新打开 Rynoa。

## 第一次使用

1. 安装后阅读“开始使用”。以后可从“设置 → 使用指南”再次打开。
2. 在“系统设置 → 隐私与安全性”允许 Rynoa 的输入监控和辅助功能。输入监控识别右 Command / Option 手势，辅助功能发送你配置的快捷键和系统动作。
3. 点击触发分组右侧的 **+** 添加手势，再选择应用、热键或系统动作；主界面只显示已配置项，点击已有动作可修改。应用/系统动作选中即保存；热键录制后点“完成”，返回则放弃修改。
4. 从一个常用手势开始尝试。主界面的“暂停触发”可以暂时停止所有动作。

关闭面板后 5 秒内重新打开，会恢复刚才的页面、搜索内容与尚未提交的热键草稿；超过 5 秒则回到主界面。应用列表会将当前绑定置顶，搜索可查找其他应用。

## 切换语言

beta.8 起支持简体中文与英文，默认跟随 macOS 语言。要单独设置 Rynoa 的语言：

1. 打开 **系统设置 → 通用 → 语言与地区**。
2. 在**应用程序**中点击 **＋**，选择 **Rynoa**，再选择 **English** 或**简体中文**；已添加时直接修改语言。
3. 完全退出 Rynoa，再从“应用程序”重新打开。

无需更改整个系统的语言。

## 键盘手势

右 Command 和右 Option 各支持单击、双击、三击、长按，可以分别设置动作。单击会在松开后短暂停顿以区分连击；与其他按键组合使用时避让正常快捷键。

更新到 beta.5 后，已有绑定保留；新增的两个单击入口默认未设置，需要自行选择动作。

## 机身拍击

支持双击、三击、四击，不区分左右。轻拍掌托或机身即可，不需要用力。设置拍击动作后才启用传感器；没有配置时显示待机，暂停时停止采样。等待“已就绪”后再试。

不同机型、桌面和姿势可能影响识别。目前硬件验收范围有限，不保证所有 Mac 都支持机身拍击。不可用时可以继续使用键盘手势。

## 不触发时

- 确认未暂停、手势已配置动作，使用的是右侧 Command / Option。
- 确认输入监控和辅助功能均已允许。系统要求退出并重新打开时，按提示操作。
- 更新后如仍无响应，核对权限列表中的 Rynoa 是否来自“应用程序”文件夹；必要时移除旧条目并重新添加当前应用。
- 系统动作依赖 macOS 的快捷键设置。外接音频设备可能不支持系统音量控制；全屏等操作也需要当前应用支持。
- 勿扰模式首次设置需要添加随应用附带的辅助快捷指令，并回到 Rynoa 完成检查。
- 机身持续显示异常或重连时，先退出再打开应用；仍失败时请反馈 Mac 型号、系统版本和复现步骤。

## 更新

仓库已从 `rynoa-releases` 更名为 `rynoa`。**beta.6 及更早版本的应用内更新检查可能误报“已是最新版本”**，beta.7 已修复更新地址；旧版请直接查看 [Releases](https://github.com/JinSooo/rynoa/releases) 获取新版本。应用不会未经你操作自动下载或安装。

测试版检查后续测试版及稳定版；稳定版只检查稳定版本。仅推荐已有 DMG 附件的发布。网络失败时可以重试或直接打开 [Releases](https://github.com/JinSooo/rynoa/releases)。

**beta.10**：设置 → 检查更新 → 下载更新 → 安装并重启。应用会校验版本、安装包完整性和开发者签名，再安装到原位置并重新打开，保留动作绑定与偏好。更新失败可重试；应用需位于可写的应用程序目录。

**beta.8 及更早版本需手动安装一次最新版本**，之后才能使用应用内更新。手动安装：退出旧 Rynoa → 下载新 DMG → 将新应用拖入“应用程序”并替换 → 重新打开。beta.7 移除了调度中心、显示桌面、应用程序窗口、上一个桌面、下一个桌面、表情与符号六项不稳定系统动作。升级会在本机备份原配置，仅清除这些动作的绑定；其他动作和偏好保留。受影响的手势可通过分组右侧 + 重新设置。请先保留旧 DMG，便于遇到问题时恢复上一版。

## 退出与卸载

在设置页选择“退出 Rynoa”。卸载前先关闭开机启动，再退出应用，将“应用程序”中的 Rynoa 移到废纸篓。卸载应用不会自动删除本地偏好。将来使用正式授权时，换机或卸载前请先解除本机绑定。

更多问题见 [支持说明](SUPPORT.md)。

beta.9 已支持应用内更新，其检查入口位于“关于”；升级 beta.10 后使用“设置”中的版本行。
